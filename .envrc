# Inherit parent .envrc (auto-pull logic from ~/Code/.envrc)
source_up

# Use the flake.nix in this directory
use flake

# Optional: Load .env file if it exists (for local secrets/config)
dotenv_if_exists
